data nat : Type := O : nat | S : nat -> nat.

define id := fix 1 f : nat* -> nat* := fun x : nat =>
             < nat > case x of
                     | O => O
                     | S x' => S (f x')
                     end.

check id.